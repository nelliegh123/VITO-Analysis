# VITO-Analysis
Code meant to be used together with ROOT files generated by the Geant4 simulations presented in the repository `*VITO-ISODLE*`. The simulations are performed for a specific setup (detectors, sample, and magnetic field), but do not assume anything about the $\beta$-decay. That information is only introduced in this step, named the Analysis. This means that a single Geant4 simulation, can be used to study multiple different decays. Only if the geometry changes, will the simulation need to be re-run.  

## Purpose 
The purpose of this code is to provide an option where Geant4 is not needed in order to evaluate the results from simulation. The workflow is the following:
1. A specific experimental setup is built in `*VITO-ISODLE*`, and a Geant4 simulation is performed
2. The resulting ROOT files are uploaded to this repository
3. The energy spectra of the $\beta$-decay to be studied is uploaded to this repository
4. The analysis is run

### 1. Upload data files
Generate your own ROOT data files 

### 2. Upload spectral data
obs keV

### 3. Run the code 
Make the code executable and run it as
```bash
   chmod +x analysis.sh
   ./analysis.sh
```

